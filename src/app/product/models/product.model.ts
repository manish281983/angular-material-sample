export interface Product {
    name: string;
    id: string;
    deleted: boolean;
    description: string;
    price: string;
    quantity: string;
    subDescription: string;
    discount: string;
    featureProduct: boolean;
    unavailable: boolean;
}